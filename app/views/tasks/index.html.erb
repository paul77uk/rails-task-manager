<h1>My tasks</h1>
<% @tasks.each do |task| %>
  <div><%= link_to task.title, task_path(task) %> <%= link_to 'Delete', task_path(task), data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %></div>
<% end %>
<%= link_to 'Add a new task', tasks_new_path %>
